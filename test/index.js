const should = require('chai').should(),
Rivalry = require('../index'),
Tournament = Rivalry.Tournament;

describe(`#Tournament`, () => {
    it(`works`, () => {
        // No test as of right now just setting up environment and making sure everything works
    });
});